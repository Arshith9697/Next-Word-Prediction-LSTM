# Next-Word-Prediction-LSTM
An end-to-end next-word prediction system built using TensorFlow LSTM networks. The project covers text preprocessing, tokenization, n-gram sequence generation, padding, model training, and inference with saved tokenizers for consistent predictions
